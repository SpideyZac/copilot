$('document').ready(() => {
  $('include').each((index) => {
    console.log(index);
    var obj = $('include:nth-of-type(' + (index + 1) + ')');
    $.get(obj.attr('src'), (data) => {
      obj.html(data);
    });
  });
});
